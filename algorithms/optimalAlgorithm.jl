function OptimalAlgo(X::AbstractArray, A::Vector{AbstractMatrix})
    d = length(A)
    if d == length(size(X))
        #hier is het gewoon die vergelijking controleren
    else #stel d < dimensie x
        #hier komt de ondergrens
end